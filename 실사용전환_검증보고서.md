# 이어:봄 실사용 전환 검증 보고서

검증일: 2026-09-17 (Asia/Seoul)

요청한 프롬프트의 코드 구현 및 로컬 검증을 완료했습니다. 실제 기관 데이터는 사용하지 않았으며 운영 배포는 하지 않았습니다. 기관의 운영 계정·D1·인증 경계와 현장 적합성 확인은 별도입니다.

## 1. 변경한 파일과 목적

프로젝트 경로: `/Users/ham1234/Documents/OTwiki/project/linkspring-main`

| 파일 | 변경 목적 |
| --- | --- |
| `app/page.tsx` | D1 기반 화면, 날짜별 후보, 편집 가능한 추출 결과, 가져오기 미리보기, 서버 저장 후 재조회, 안내 완료 분리 |
| `app/globals.css`, `app/layout.tsx` | 기존 디자인 유지, 좁은 화면·긴 대화상자 스크롤·키보드 초점 보완, 사회사업 확장 소개 |
| `app/api/state/route.ts` | 인증 후 조회·변경 진입점 |
| `lib/state-actions.ts` | 입력 검증, 후보 재검증, 중복 배정 차단, 명단·시간표 일괄 반영 |
| `lib/state-store.ts` | 일관된 조회, 상태 버전 비교, 데이터와 감사 로그 원자적 저장 |
| `lib/domain.ts` | 서울 날짜·회기 검증, 후보 근거와 동률 정렬, 일정 토큰 추출 |
| `lib/csv.ts` | UTF-8/EUC-KR, 필수 열·중복·날짜·행 수 검사, 가명 필드 변환 |
| `lib/access.ts`, `lib/server.ts`, `lib/errors.ts` | 허용 계정, 동일 출처 검사, 운영에서 개발 인증 우회 차단, 입력 크기 제한, 안전한 오류 응답 |
| `app/api/ai/parse-absence/route.ts`, `lib/absence.ts` | 원문 요청 거부, 서버 Claude 호출, 도구 JSON 검증, 15초 제한, 명시적 규칙 분석 보완 |
| `db/schema.ts`, `db/index.ts` | 버전 필드·트랜잭션 가드 스키마와 D1 타입 |
| `drizzle/0005_misty_boom_boom.sql`, `drizzle/0006_atomic_guard.sql`, `drizzle/meta/*` | 기존 마이그레이션을 보존한 추가 마이그레이션. 오래된 버전으로 쓰면 트랜잭션 전체 취소 |
| `vite.config.ts`, `wrangler.local.jsonc` | 루프백 개발서버, 명시적 개발 인증 모드, 로컬 D1 마이그레이션 |
| `package.json`, `package-lock.json`, `tsconfig.json` | 손상된 lockfile 복구, macOS 실행 명령, 테스트·타입 도구 |
| `.env.example`, `.gitignore` | 필요한 환경변수 예시, 환경 파일·로컬 DB·복구용 파일 제외 |
| `public/examples/participants.csv`, `public/examples/schedule.csv` | 가상 데이터로 된 표준 입력 양식 |
| `tests/core.test.ts`, `tests/d1.test.ts`, `tests/*.test.mjs` | 업무·실제 D1 엔진·HTTP·기존 UI 계약 검증 |
| `README.md`, `readme_submission_v1.1.md` | 설치·사용·CSV·보안 설명 갱신 및 미검증 주장 정정 |

기존 `.env.local` 비밀값과 `.openai/hosting.json`의 사이트 ID·DB 바인딩은 변경하지 않았습니다. 손상된 기존 lockfile은 `package-lock.corrupt.backup.txt`에 보존했습니다.

## 2. 실제 사용자 흐름 기준 완료 기능

1. 시간표와 가명 명단을 검증하고 반영합니다. 데이터가 없으면 빈 화면을 표시하며 데모 명단을 자동으로 넣지 않습니다.
2. 날짜별 신청이 있으면 해당 날짜의 활성 신청과 서비스·시간 범위를 사용합니다. 그렇지 않으면 정기 요일·가능 시간을 사용합니다.
3. 전체 30분이 맞는 후보를 우선 표시하고 대기 기간 내림차순, 최근 연결 오름차순(없음 우선), 가명 ID 순으로 정렬합니다. ±30분·±1시간 대안은 재협의용이며 그대로 확정할 수 없습니다.
4. 문자 원문은 브라우저에서 일정 토큰으로 추립니다. 담당자 확인 후에만 Claude로 보내고 누락·오류는 규칙 분석과 직접 입력으로 처리합니다. 상대 날짜 기준은 서버의 서울 현재 날짜입니다.
5. 수정한 날짜·시간·서비스·담당자 코드를 서버에서 다시 검증합니다. 시간표에 없는 회기, 점심·잘못된 시간, 이미 시작한 회기는 거부합니다.
6. 최종 확정 직전에 최신 데이터로 가능 여부를 재검증합니다. 버전 변경·동일 회기 확정·대기자 시간 중복을 차단합니다.
7. 버전 갱신·업무 데이터·감사 로그는 단일 D1 트랜잭션입니다. 저장 중 오류는 모두 되돌립니다. 저장 성공 뒤 새로 읽은 상태만 화면에 반영합니다.
8. 연결과 실제 안내 완료를 분리하고, 개인정보 없이 날짜·시간·서비스만 포함한 문구를 복사합니다. 발송은 기관 채널에서 직접 수행합니다.

## 3. 실행한 검증과 결과

| 검증 | 최종 결과 |
| --- | --- |
| `npm run typecheck` | 통과 |
| `npm run lint` | 통과 |
| `npm run build` | 통과. Sites build helper가 프로젝트 build 스크립트를 실행 |
| `npm test` | 20개 통과: 핵심 19개 + 실제 로컬 D1 통합 1개 |
| `npm run test:ui` | 6개 통과: HTTP 2개 + UI/CSS 계약 4개 |
| `npm run db:local` | 0000~0006 마이그레이션 로컬 적용 성공 |
| 개발서버 `GET /` | HTTP 200 |
| `GET /api/state` | HTTP 200, 빈 운영 목록 정상 조회 |
| 외부 Origin의 상태 변경 | HTTP 403 |
| 잘못된 상태 변경 입력 | HTTP 400 |
| 식별정보 형식이 섞인 AI 요청 | HTTP 400, 입력 내용 미반환 |
| 실제 Claude API | 가상 일정 1건으로 HTTP 200, `source=claude`, 날짜·시작·종료·서비스·담당자 코드 확인 |
| 비밀값 번들 검사 | 빌드 산출물 45개에 설정된 실제 API 키 포함 0건. 키 자체는 출력하지 않음 |

핵심 테스트는 100명 후보 및 CSV 일괄 반영, 서비스·활성·요일·날짜별 신청, 동률 정렬, 30분 전체 가능 시간, 점심·서울 자정·이미 시작한 회기, 중복 배정, 오래된 버전, 저장 실패 시 감사 로그까지 취소, Claude 누락/잘못된 JSON/통신 실패, 인증·출처 검사를 포함합니다.

실제 로컬 D1 통합 검사는 별도 임시 DB에서 모든 마이그레이션 → CSV 반영 → 결석 등록 → 연결 확정 → 오래된 버전의 변경 거부 → 엔진 종료·재시작 → 연결 및 감사 로그 복원을 실행했습니다. 테스트 DB는 종료 후 정리했으며 사용자 개발 DB에는 가상 명단을 넣지 않았습니다.

검증 중 수정한 환경 문제:

- 기존 `package-lock.json`에 출력 잘림 문구가 들어가 설치할 수 없어 정상 재생성했습니다.
- Linux에 종속된 실행 스크립트를 macOS에서 동작하는 명령으로 바꿨습니다.
- 테스트 실행기의 내부 소켓 및 로컬 D1 실행은 샌드박스 밖 실행 권한으로 검증했습니다.
- 로컬 Worker 바이너리가 지원하지 않는 미래 호환 날짜를 지원 날짜인 `2026-05-22`로 맞췄습니다.
- 기존 UI 카탈로그 테스트가 사용하지 않는 CSS도 요구하므로, 현재 앱에서 실제 사용하는 스크롤·초점·애니메이션 계약으로 갱신했습니다.

Vinext가 `/` 경로의 정적/동적 분류를 자동 판별하지 못했다는 안내와 Node 내장 SQLite의 실험 기능 안내가 출력됩니다. 빌드와 테스트는 성공했습니다. 브라우저 클릭·시각 QA 및 기관 현장 테스트를 수행했다고 주장하지 않습니다.

## 4. 필요한 환경과 외부 연동

- 로컬 실행 주소: 개발서버가 출력한 `http://127.0.0.1:5173/`.
- 운영은 Sites ChatGPT 로그인 게이트웨이 및 `COORDINATOR_EMAILS` 허용 계정이 필요합니다. 기본값은 접근 거부입니다.
- 운영 D1 `DB` 바인딩과 0000~0006 마이그레이션 적용이 필요합니다. 로컬 설정 파일의 placeholder ID를 운영 DB ID로 사용하지 않습니다.
- `CLAUDE_API_KEY`, `CLAUDE_MODEL`은 운영 서버의 비밀값/환경변수로 별도 설정합니다. 현재 로컬 키의 실제 호출 성공은 확인했습니다.
- `LOCAL_DEV_AUTH=1`은 루프백의 가상 데이터 검증에만 사용하며 운영에서는 `0`입니다.
- EMR·기존 예약 시스템·문자 발송은 아직 연동하지 않았습니다. CSV 반영 및 담당자의 실제 발송 절차를 사용합니다.

## 5. 실사용 전 확인할 위험과 남은 한계

기관 담당자가 허용 계정 로그인·비허용 계정 차단, 인증 헤더 위조 차단, 운영 D1 백업·복구, 기관의 가명 ID 관리 및 데이터 처리 절차를 확인해야 합니다. 공개 Worker를 Sites 인증 게이트웨이 없이 직접 노출하는 운영은 지원하지 않습니다.

시간표는 30분 회기 및 지정 운영 시간 기준입니다. 60분 상담이나 집단 정원 등 다른 운영 형태는 추가 설계가 필요합니다. 다른 예약 시스템의 중복은 자동 검사하지 않습니다. 확정 취소·완료 이력 수정 화면 및 보존기간별 정리 절차는 제공하지 않습니다. 날짜별 신청 상태는 CSV 재반영으로 갱신할 수 있습니다.

원문에서 추출한 일정 토큰도 전송 전에 담당자가 확인해야 합니다. 실명·생년월일로부터 ID를 만들지 않으며, 가명 데이터는 완전 익명 데이터가 아닙니다. 실제 현장 적합성·추출 정확도·업무 시간 절감 효과는 아직 측정하지 않았습니다.
