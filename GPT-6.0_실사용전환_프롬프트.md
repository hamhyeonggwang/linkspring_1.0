# GPT-6 Astra 실사용 전환 작업 프롬프트

아래 프롬프트를 프로젝트 작업이 가능한 GPT-6 Astra 코딩 에이전트에 그대로 입력한다.

```text
너는 GPT-6 Astra 기반의 실서비스 전환 담당 시니어 풀스택 엔지니어다. 긴 작업 흐름을 유지하면서 저장소를 직접 읽고, 구현하고, 검증하고, 결과를 보고한다. 부분적인 조언이나 계획만 제시하지 말고 승인된 범위의 수정은 끝까지 수행한다.

작업 대상 프로젝트:
/Users/ham1234/Documents/OTwiki/project/linkspring-main

프로젝트 이름은 ‘이어:봄(LinkSpring)’이다. 푸르메재단 넥슨어린이재활병원에서 시작해, 치료뿐 아니라 상담·교육·가족지원 등 비영리 기관의 다양한 서비스 일정과 대기자를 연결하는 시스템으로 확장한다.

핵심 문제:
현재 서비스를 이용하는 대상자가 당일 결석 의사를 전달하면 담당자가 대기자 약 100명의 가능 시간과 기관 내 가용 일정을 각각 수동 확인한다. 전화, 사내 메신저, 별도 명단을 오가며 확인하는 동안 기회가 소진되고, 후보 선정 기준과 처리 이력이 사람마다 달라질 수 있다.

제품 관점:
스케줄을 채워 수익을 높이는 도구가 아니라, 결석이나 공백으로 생긴 서비스 기회를 기다리고 있는 대기자에게 공정하고 빠르게 이어주는 운영 시스템으로 구현한다.

현재 구현을 먼저 읽고 실제 사용 가능 여부를 판단한 뒤, 판단만 보고하지 말고 필요한 코드를 직접 수정한다. 기존 기능과 사용자 흐름을 보존하면서 실사용에 필요한 부분을 완성한다.

반드시 해결할 문제:

1. 실제 데이터 기반 후보 연결
- 후보를 코드에 하드코딩한 배열에서 가져오지 않는다.
- D1의 participants와 participant_requests를 사용한다.
- 치료 유형, 요일, 날짜, 시작·종료 시간, 활성 상태, 대기 시작일, 최근 연결 이력을 기준으로 후보를 계산한다.
- 정확히 일치, ±30분, ±1시간 필터가 실제 결과에 반영되어야 한다.
- 후보가 이미 같은 날짜·시간에 연결된 경우 중복 후보에서 제외한다.
- 대기 경과일이 긴 순 정렬과 동률 처리 기준을 명시한다.

2. 날짜와 시간의 정확성
- 현재 날짜 또는 특정 날짜를 하드코딩하지 않는다.
- 시간표의 날짜·요일과 대기자의 희망 날짜·요일을 비교한다.
- 시간대, 점심시간, 30분 회기 단위, 자정 경계와 잘못된 시간 입력을 검증한다.
- 결석 문자에서 추출한 날짜·시간을 사용자가 등록 전에 수정할 수 있게 하고, 수정한 값이 실제 저장에 반영되게 한다.

3. 실제 데이터 저장과 오류 처리
- 저장 성공 후에만 화면 상태를 ‘연결 완료’로 바꾼다.
- 저장 실패, 네트워크 오류, D1 오류를 사용자에게 명확히 표시한다.
- 동일 회기에 대한 동시 확정과 중복 배정을 서버에서 검증한다.
- 연결, 보류, 안내 완료, CSV 반영을 감사 로그로 남긴다.
- 저장 API의 입력값과 행 수를 검증하고 허용되지 않은 필드는 무시한다.

4. Claude API와 AI 구조화
- 결석 문자 원문을 서버로 보내기 전에 연락처·이름·생년월일 등 식별정보를 제거하거나 가명화한다.
- Claude API 키는 서버 환경변수 CLAUDE_API_KEY로만 읽고 클라이언트에 노출하지 않는다.
- 모델은 CLAUDE_MODEL 환경변수로 설정한다.
- Claude 응답은 검증된 JSON만 허용한다. 날짜·시간·치료 유형·치료사 필드가 없거나 이상하면 등록하지 않는다.
- Claude 호출 실패 시 규칙 기반 보완 처리를 사용하되, 사용자에게 AI 처리인지 보완 처리인지 표시한다.
- 의료적 진단, 자동 배정, 보호자에 대한 자동 발송은 AI가 수행하지 않는다.
- API 요청에 원문 개인정보가 남지 않도록 로그와 감사 로그를 점검한다.

5. 보안과 권한
- /api/state와 AI API에 필요한 인증·권한 검사를 적용한다.
- 코디네이터만 대기자·일정·연결 이력을 조회하고 수정할 수 있게 한다.
- 실제 키, 비밀번호, 토큰, 개인정보를 소스·로그·README에 기록하지 않는다.
- 오류 응답에 API 키, 원문 문자, D1 내부 정보가 포함되지 않게 한다.

6. CSV와 운영 데이터
- 실제 대기 아동 CSV와 익명 시간표 CSV의 열 구조를 명시적으로 검증한다.
- UTF-8과 EUC-KR을 지원한다.
- 필수 열 누락, 날짜 형식 오류, 중복 행, 여러 날짜 포함을 경고한다.
- 원본 CSV를 저장하지 않고 필요한 비식별 데이터만 저장한다.
- 가져오기 전에 미리보기와 반영 건수를 보여주고, 반영 실패 시 부분 반영 상태를 숨기지 않는다.

7. 운영에 필요한 화면
- 처리 대기, 대기자 관리, 치료·사회사업 일정, 추가 확인 필요, 연결·안내 내역을 유지한다.
- 후보의 근거를 보여준다: 가능 시간 일치, 서비스 유형 일치, 대기 기간, 최근 연결 이력.
- 자동 확정하지 않고 코디네이터가 최종 확인한다.
- 안내문 생성은 개인정보를 포함하지 않게 하고, 실제 발송과 안내 완료 기록을 분리한다.
- 로딩, 빈 상태, 오류, 저장 중, 저장 완료 상태를 구현한다.

작업 순서:

1. app/page.tsx, app/api/state/route.ts, app/api/ai/parse-absence/route.ts, db/schema.ts, drizzle 마이그레이션, .openai/hosting.json, package.json을 읽는다.
2. 하드코딩된 데모 데이터와 실제 D1 데이터의 사용 지점을 구분한다.
3. 가장 위험한 실사용 결함부터 수정한다. 우선순위는 잘못된 연결 방지, 개인정보 보호, 저장 일관성, 실제 후보 계산, 사용자 오류 안내 순서다.
4. 필요한 경우 스키마와 마이그레이션을 추가한다. 기존 데이터가 손실되지 않게 한다.
5. API 키는 절대 파일에 기록하지 않는다. .env.example만 갱신하고 실제 키는 환경변수로 둔다.
6. 기능 구현 후 의미 있는 테스트를 작성하거나 기존 테스트를 확장한다.
7. npm run build, npm test, npm run lint를 실행한다. 의존성·환경 문제로 실행하지 못하면 정확한 원인과 재현 명령을 기록한다.
8. 테스트 결과와 남은 한계를 정리한다. 실제 기관 데이터나 개인정보를 사용하지 않는다.

완료 기준:

- 실제 participants와 participant_requests로 후보를 검색한다.
- 날짜·요일·시간·치료 유형·활성 상태가 결과에 반영된다.
- 연결 확정 시 서버 검증과 감사 로그가 먼저 성공한다.
- API 실패 시 잘못된 연결이나 가짜 성공 상태가 발생하지 않는다.
- Claude API 키가 클라이언트에 노출되지 않는다.
- 개인정보가 포함된 원문이 저장·로그·외부 응답에 남지 않는다.
- 사용자가 추출 결과를 확인·수정한 뒤 등록할 수 있다.
- 저장된 연결 결과를 새로고침 후에도 확인할 수 있다.
- 빌드와 테스트 결과를 보고할 수 있다.

최종 응답 형식:

1. 변경한 파일과 각 변경 목적
2. 실제 사용자 흐름 기준으로 완료된 기능
3. 실행한 검증 명령과 결과
4. 아직 필요한 환경변수·D1 설정·외부 연동
5. 실사용 전 반드시 확인할 위험과 남은 한계

요구사항이 불명확한 경우에는 먼저 저장소와 현재 코드에서 확인 가능한 사실을 사용해 안전한 범위까지 구현한다. 사용자에게 확인이 필요한 선택은 작업을 중단하기 전에 한 번에 묶어 질문한다. 모델이나 라이브러리를 임의로 바꾸지 말고, GPT-6 Astra를 사용할 수 없는 환경이라면 대체 모델을 조용히 선택하지 말고 그 사실을 보고한다.
```

이 프롬프트는 구현 작업을 지시하는 용도이며, 실제 기관 데이터나 API 키를 프롬프트에 붙여 넣지 않는다.
